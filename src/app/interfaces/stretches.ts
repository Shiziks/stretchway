export interface Stretches {
    area:{
        name:string,
        image:string,
        description: string[],
        benefits?: string[],
        sides:boolean
    }
}

export interface Area{
    name:string,
    image:string,
    description: string[],
    benefits?: string[],
    sides:boolean
}
